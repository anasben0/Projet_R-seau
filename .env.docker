# Configuration Docker Compose pour PostgreSQL
# Ce fichier peut être commit dans Git (pas de données sensibles en prod)

# Base de données
POSTGRES_DB=polyrezo_db
POSTGRES_USER=polyrezo_user
POSTGRES_PASSWORD=SecurePassword@2025!

# Port exposé (ne pas changer si le backend utilise 5433)
POSTGRES_PORT=5433
